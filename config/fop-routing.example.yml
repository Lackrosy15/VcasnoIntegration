# Copy to config/fop-routing.yml to enable these correspondences.
# Tokens are stored only in .env, never in this file.
integration:
  kommo:
    fop-mappings:
      - value: "Зелинская"
        fop-id: fop1
      - value: "Ветров"
        fop-id: fop2
